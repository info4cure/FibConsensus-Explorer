# FibConsensus Explorer

Interactive research preview for FibConsensus v2.2.

This deployment bundle contains only the application code and static assets
required to run the explorer. Database access is read-only and credentials are
provided through deployment environment variables.

Scientific database release: v2.2
Application preview: v0.2.0-preview
